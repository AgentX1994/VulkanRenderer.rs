{
    "tasks": [
        {
            "type": "shell",
            "label": "Rust: build",
            "command": "cargo",
            "args": [
                "build",
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$rustc"
            ],
            "group": "build"
        }
    ]
}